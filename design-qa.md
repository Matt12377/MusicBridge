# 樱花毛玻璃正式应用修订验证

## 2026-09-04 侧栏设置横条与网易云歌单折叠

- Base：`d7c6bbef72c40ae71d7c72073e992cd8467cbb92`；实现：`cab86c5`；结果报告身份由本节所在独立提交确定，下一基线为报告 HEAD。分支继续为 `codex/v3-ui-redesign`，不合并或发布。
- 按 UI 设计技能保留原生按钮、展开语义、键盘焦点与现有玻璃材质。仅移除侧栏头像／昵称；设置中的账户信息、登录管理和全部业务入口保持不变。折叠不发起额外请求、不清空歌单；会话内切换侧栏宽窄保留折叠状态，未新增跨启动持久化。

| Before | After | 原因 |
| --- | --- | --- |
| 底部网易云头像与昵称 | 全宽“设置”毛玻璃横条，窄栏显示设置图标 | 将底部入口改为通用设置 |
| 歌单标题不可折叠 | “网易云歌单”按钮及展开箭头 | 控制侧栏歌单占用空间 |
| 窄栏弹层被父级滚动区域裁切 | 弹层传送至 body，按视口定位并限制尺寸 | 避免滚动容器裁切，保留原选择行为 |

- TDD：四项实际 Vue 组件行为测试初始 RED，实现后 GREEN；原生截图另发现窄栏弹层裁切，新增脱离滚动容器断言 RED→GREEN。验证包括默认展开、收起／恢复、列表选择、错误重试、加载／空态折叠、侧栏宽窄切换、设置无头像及当前页语义。
- 新鲜证据：桌面单测 **682/682**、桌面类型检查、生产构建、control-plane、boundaries、`git diff --check` 均 exit 0。外置构建日志为 `musicbridge-sidebar-{unit,type,build}.log`；完整 Playwright E2E **未运行**，现有用例入口与断言已同步并经过类型检查。
- 使用真实生产 dist 的隔离合成 Electron，检查设置跳转、底部无头像、歌单展开／收起；1440×819 和 720×480 均确认弹层在视口内且中心命中实际弹层，不只检查 DOM 存在。Esc 关闭并回焦到触发按钮通过，原生进程正常退出。截图 `prototypes/sakura-glass/.qa/sidebar-{expanded,folded,settings,compact-popover,minimum}.png` 留在本地忽略目录，已目视核对展开与宽／窄弹层。
- 没有重启或操控用户真实开发实例，没有连接真实账号或触发真实音频。既有合成播放截图超时、真实播放及 Owner 最终视觉／产品验收维持原状态，本轮不扩展至播放修复。

## 2026-09-04 通透度与柔暗微调

- Base：`0127f6214ba256e5623077b6535a701749609345`；实现：`549569f`；结果报告身份为本节所在独立提交，下一轮基线为报告 HEAD。
- 范围仅 CSS 及样式回归。新增透明度/柔暗底色/深封面文字对比度断言按 RED→GREEN 执行。678/678 桌面单测、生产构建、control-plane、boundaries、diff-check 全部 exit 0；本轮未重跑类型检查（无生产 TypeScript 变化）。外置日志为 `musicbridge-glass-dim-{red,unit,build}.log`。

| Before | After | 原因 |
| --- | --- | --- |
| 面板遮盖 30%–60% | 20%–44% | 更多封面色彩透过玻璃 |
| 控件遮盖 24%–64% | 16%–48% | 减少乳白实心观感 |
| 内容底纯白 58% | 偏灰白 42% | 降低大片亮白区域 |
| 背景浅白 wash | 灰紫 wash | 稍微压暗，不改成深色主题 |

- 遵循 UI 设计技能的对比度要求同步加深文字，保留颗粒、blur、边缘和现有布局。实际生产 dist 的隔离未播放截图 `.qa/glass-dim-idle.png` 已目视核对。
- 原生合成播放截图等待超时，当前不再继续重试该验证脚本；只确认未播放视觉与自动软件检查，不宣称播放/暂停视觉验收通过。隔离验证窗口已退出，未操作真实播放实例。Owner 最终观感确认仍待反馈。

## 2026-09-04 后续反馈：封面消失与磨砂不足

- 本轮 Base：`45b94081686b319c3930db84da7baf168859f55c`；实现提交：`6f5f4f7`。独立报告提交为本节所在提交；后续继续以该报告 HEAD 为基线。
- 根因：`sakura-theme.css` 将封面 opacity 压为 `.10` 并使用 48px 模糊，固定渐变占主导；App 仅在 `playing` 时传入封面，暂停时移除。
- 修复：封面 opacity `.82`、12px 自身模糊、放大 1.08，保留封面构图大轮廓；浅色 wash 保证底色。结构玻璃进一步模糊，使用本地静态 SVG 细颗粒与明暗内边缘。纹理不动画、不覆盖文字、不拦截点击，减少透明效果时关闭。
- 文字增加深色封面保守底色对比度断言，发现 RED 后加深次级文字，GREEN；新增封面绑定及材质测试 RED→GREEN。
- `test:unit` 677/677、桌面类型检查、生产构建、control-plane、boundaries、diff-check 均 exit 0。日志为外置构建 tmp 中的 `musicbridge-cover-frost-{unit,type,build}.log`。
- 真正生产 dist 的隔离合成窗口已经渲染封面背景；目视检查 `.qa/cover-frost-playing.png`，封面轮廓、面板颗粒、细边框和深色文字可见。与之前只有固定渐变的截图区分明显。
- 暂停自动验证未收口：第一轮测试脚本误用了“继续播放”标签（真实组件为“恢复播放”）；修正脚本后第二轮在播放/图片就绪等待阶段超时。没有因此改动生产播放逻辑，也没有继续反复重试；暂停保留有源码回归证据，但不声明原生暂停视觉检查通过。
- 真实应用未被重启或操作；原生截图是合成曲目、示意封面，不是真实账号/音频验收。Owner 对最终磨砂强度仍待反馈，旧的正式验收边界不变。

## 身份与结果

- 分支：`codex/v3-ui-redesign`。
- Base：`f5246a17f78180c4c355e1979154b259db87432a`。
- 实现提交：`e3942a3`；本文件及 STATUS/TODO 为后续独立结果报告提交，报告身份由包含本文件的 Git 提交确定。
- 本轮结果：软件检查通过；主界面材质聚焦目视检查通过；Owner 对修订效果的接受仍待反馈。
- 下一步基线：本分支报告提交后的 HEAD；暂不创建下一任务，不合并或发布。

## 范围与参考

参考为已获认可的 `prototypes/sakura-glass/index.html`，沿用樱粉、淡紫、淡蓝和玻璃材质，而非复制原型的模拟业务。正式应用的按钮入口、组件事件、IPC、播放及资料库数据流保留。

本轮收到“边栏及控件又做成纯色”的纠正，改为独立透色玻璃材质：结构面板采用低遮盖渐变、28px 背景模糊和白色内边缘；小控件使用独立透色层和 14px 背景模糊。密集歌曲列表维持较厚底色保证文字可读性。虚拟列表行高维持原合同，未给每行添加模糊层。

按 UI 设计技能执行参考对照、深浅色文字检查和有限结构面板模糊；没有把第三方示意封面或合成数据写入生产资源。

## 新鲜自动证据

以下命令均在 UI 工作树、Node 22.23.2 下执行，结果 exit 0：

| 检查 | 结果 |
| --- | --- |
| 桌面 `test:unit` | 675/675，通过，无跳过 |
| 桌面 `typecheck` | 通过 |
| 桌面生产 `build` | 通过 |
| `verify-control-plane.mjs` | CONTROL_PLANE=PASS |
| `verify-boundaries.mjs` | BOUNDARIES=PASS |
| `git diff --check` | 通过 |

新增主题及原生浅色窗口断言按 RED→GREEN 执行；用户反馈的透色材质另有 RED→GREEN 回归。完整单测曾发现收藏组件测试工具将原始 props 传给编译模板，遗漏 Vue `withDefaults`；改为传已解析的 `instance.$props`，不修改收藏业务逻辑。

构建、单测及类型日志位于外置构建临时目录，分别为 `musicbridge-sakura-glass-build-20260904.log`、`musicbridge-sakura-glass-unit-final-20260904.log`、`musicbridge-sakura-typecheck-final-20260904.log`，不将本机运行日志纳入 Git。

## 视觉证据与边界

使用真正的生产 dist 启动独立 Electron 验证窗口，隔离 userData、mock keychain、合成 Core/Roon，窗口标题明确标识合成数据。未连接真实账号或触发真实音频；既有真实开发实例不替换。

在相同的 1440×819 内容尺寸下，将已认可的原型与正式应用“我喜欢的音乐”截图放在同一对照页面检查。生产截图保留在本地忽略目录 `prototypes/sakura-glass/.qa/capture-1788532683266.png`，对照页为 `.qa/production-comparison.html`。本轮有意增强边栏及底栏透色强度，以落实 Owner 最新反馈；原型的不同曲目、封面、过滤功能和播放器展示不作为生产逻辑迁移目标。

| 之前 | 修订后 | 原因 |
| --- | --- | --- |
| 结构面板偏乳白实色 | 边栏粉紫蓝透色，底栏透出环境色 | 落实毛玻璃而非纯色面板 |
| 小控件缺乏玻璃层次 | 搜索、音质、设备、队列等控件透色与边缘高光 | 强化独立玻璃表面 |
| 深色原生标题栏 | 浅色标题栏和启动底色 | 避免内容与系统窗口割裂 |

未声称完成全页面、所有窗口尺寸或真实设备验收。本轮最终目视聚焦主界面、边栏和底栏；其余复杂表单、720px 最小窗口及真实长名称内容的完整人工验收仍待进行。已有远程音频恢复、真实播放、硬件录音、容量、合并、签名公证和发布外部门全部不升级。
