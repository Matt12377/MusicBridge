# 背景双状态预览检查

- 日期：2026-09-05。
- Base：`fffc12783dc05ea2f2521288685f6cce3c6caf2a`；实现：`a8a2bf0`；报告身份为本文件所在提交，下一基线为报告 HEAD。
- 范围：独立 HTML 概念预览；不迁移正式应用，不连接真实服务，不合并、不发布。
- source visual truth：用户的 MusicBridge `截屏2026-09-05 15.07.28.png`（4096×2304）以及两张 ECHO 参考图；保留 MusicBridge 五列、两分类侧栏与居中悬浮播放器，ECHO 仅提供连续背景与分层材质参考。
- 原始 MusicBridge 路径：`/var/folders/_n/tl8_8vzx5ng2xr633gkh0cn80000gn/T/TemporaryItems/NSIRD_screencaptureui_VsKYIz/截屏2026-09-05 15.07.28.png`。本轮已与实际预览图片在同次工具输入中查看对照。
- 实现截图目录：`/Volumes/LifeWeave/Developer/CommandLine/tmp/musicbridge-ambient-study-20260905/`；包括 `idle.png`、`playing.png`、`list-playing.png`、`narrow-idle.png`、`desktop-final.png`。
- 实测 CSS viewport：首轮 1309×909，最终桌面 1440×1000，窄窗 720×720。最终桌面截图 1440×1000；窄窗截图由浏览器缩放呈现。浏览器画面存在缩放及留白，不做逐像素、字形抗锯齿或与原生窗口相同密度的结论。

## 视觉检查

- 全图：未播放有粉紫/雾蓝连续曲面轮廓；选曲后背景来源改为当前封面；主页板块无独立容器气泡；播放器仍居中悬浮，侧栏全高。
- 字体：沿用 macOS 系统中文字体，主页歌名 15px、歌手 12px，标题与辅助说明有层级；不复制参考图中的像素字体。缩放截图不作为精确字形证据。
- 布局：1440 宽度为五列，每列约 205px；720 宽度为三列，页面无横向溢出，播放器左右边界约 12/708px，底边约 708px。保留滚动底部避让。
- 材质：默认遮罩 30%，选曲后 60%；侧栏与播放器分别承托内容，列表采用连续浅色底。当前照片数据下未观察到重大阅读阻断；不宣称所有真实专辑封面均完成对比度验收。
- 图像：默认背景为生成的真实位图，封面复用已有本地示意照片，页面图片加载检查无破图。素材内容与用户真实音乐不同，是明确的概念预览差异。
- 文案：顶部标明示意曲目、不播放音频；非核心页面点击会提示预览范围。与参考的界面文案变化是有意的示意化，不冒充真实资料库。
- 聚焦检查：播放器状态文字、暂停按钮、队列和侧栏设置的可访问名称通过实际 DOM 检查；未进行精确像素裁切比较，原因是本轮为概念方向验证，非参考图复刻。

## 交互与修订

- 实际浏览器验证默认→播放、前后切歌、暂停保留同一个封面 src、选中封面再次点击暂停、返回未播放、主页/收藏列表和队列展开。
- 第一轮发现窄栏设置按钮需补充可访问名称、当前封面暂停图标应执行暂停；均已修复，第二轮通过名称与播放按钮状态检查。
- 未播放返回动画结束后核实两背景层 opacity 分别为 1/0，可见层为默认画面；暂停不触发背景重建。
- 控制台采集 error/warn 为空；脚本语法检查 exit 0，Git diff-check exit 0。
- 未跑生产完整测试、真实 Electron/账号/音频测试；本轮仅预览，既有生产验收和播放恢复 carryover 不变。
- 细化候选：后续使用真实且色彩更复杂的专辑封面再调整遮罩；生成曲面的审美接受由用户决定。

final result: passed

此结果仅指独立预览可供视觉评审及上述聚焦交互检查通过，不等于 Owner 认可背景方向或正式产品验收。
