# 樱花玻璃预览检查记录

日期：2026-09-04。范围：独立 HTML 交互与风格预览，不是正式应用验收。

- 分支：`codex/v3-ui-redesign`
- 基线：`87756a7306b67a66af836a5aa52895de06d83ed3`
- 实现提交：`245fb89`（仅预览目录）
- 报告提交：包含本文的后续 `docs(ui-preview)` 提交；不在文件内自引用 SHA。
- 本地入口：http://127.0.0.1:4178/
- 原图与 HTML 同屏对照：http://127.0.0.1:4178/.qa/comparison.html （仅本机，参考截图不进入 Git）。

## 检查结果

| 检查 | 证据与结论 |
| --- | --- |
| 搜索 | 浏览器输入“海风”，DOM 中歌曲行从 12 行筛为 1 行；切换导航后恢复 12 行。通过。 |
| 下一首与队列 | 点击下一首后当前曲目变为“海风经过”；展开队列后显示后续曲目，底部播放器同步。通过，仅模拟。 |
| 播放与进度 | 点击暂停后按钮变为开始模拟播放；进度控件键盘右移更新 value 与时间文字。通过；未声称真实音频 seek 或鼠标拖动验收。 |
| 导航 | 专辑显示 6 张示意卡片，歌曲列表隐藏；返回我喜欢的音乐恢复列表。通过。 |
| 连接弹窗 | 明确显示 Core、Roon、网易云未连接，并说明独立设计预览。通过。 |
| 窗口适配 | 在浏览器实际 CSS 视口 521×933 和 1309×872 检查 `scrollWidth <= innerWidth`。无横向页面溢出。 |
| 运行错误 | 本次浏览器 error / warn 日志为空。 |
| 静态检查 | 内联脚本 `node --check`、STATUS JSON 解析、`git diff --check` 通过。 |

## 视觉对照及边界

已在同屏对照页查看用户 ECHO 原图与当前 HTML：继承樱粉/淡蓝背景、乳白歌曲面板、透明侧栏和底部播放器；保留 Music Bridge 自己的导航层级与系统中文字体，不复刻 ECHO 品牌、像素字体或曲目。

首轮修正了通用 hidden 被布局 display 覆盖的问题，并缩短品牌副标题、避免品牌名称折行。桌面与窄窗的浏览器截图已在当前任务中查看。

视觉风格初审：PASS（仅整体色彩和分层）。精确像素对比：BLOCKED，浏览器视口覆盖后的截图存在缩放/多余边缘，不能用它宣称像素级对齐或文字清晰度验收。临时视口覆盖已复原，不为截图问题扩大测试循环。

用户视觉验收：PENDING。仍需用户在实际浏览器宽窗口确认粉色浓度、玻璃透明度和信息密度。正式应用功能不在此预览中重写；收藏、录音等入口仅展示布局占位。未运行整个产品构建、真实账号、Roon、音频或库存导入测试。

## 后续与提交边界

预览和本记录可随当前 UI 分支供代码评审；不得升级既有真实播放、设备、签名、公证、合并或发布状态。下一阶段若获准实施，基于本 UI 分支的最终报告提交继续，先确认视觉，再迁移正式组件与执行对应回归。
