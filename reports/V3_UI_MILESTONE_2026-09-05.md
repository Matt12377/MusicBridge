# UI 里程碑基线 — 2026-09-05

Owner 已确认当前 UI 完成一个里程碑，要求将当前完成产物提交到基线；下一阶段为整个实物收藏 UI 重制。

## 提交身份

- 原基线 `codex/v3-ui-baseline`：`87756a7306b67a66af836a5aa52895de06d83ed3`。
- 已认可成果 `codex/v3-ui-redesign`：`86d614496dc114bf750229eed1397da3a8e53f7f`，最近生产实现 `1bfc664280ccd775ea5181d60fe675b2affeb0aa`。
- 本报告及 AGENTS/STATUS 收尾提交作为新基线，身份通过 `git rev-parse ui-milestone-2026-09-05^{commit}` 解析。
- 两分支通过快进保持相同 HEAD，并以普通原子推送同步到 origin；标签 `ui-milestone-2026-09-05` 保留不可变的阶段定位。完成判据为远端两分支与标签解引用 SHA 均等于本报告提交。
- 保留全部实现/报告历史，不 squash、不强推，不合并 main。原基线 SHA 继续保留在历史中。

## 本轮完成范围

- 全窗口连续背景、当前封面背景及暂停保留、手动浅色/蓝灰深色主题。
- 首页大封面墙、去容器底板、收紧页面边距、统一歌曲列表和元数据层级。
- 紧凑悬浮播放栏、当前真实音质、居中短进度、连续进度时钟、拖动时实时音量请求。
- 最终侧栏：主页、专辑、艺术家、流派、收藏（仅 Roon）、实物收藏、录音、歌单（仅网易云且可折叠）；歌单导航样式统一，设置距底边 16px。
- 旧预览的尺寸参数和此前视觉待确认文字属于阶段历史，当前视觉以本里程碑为准。

## 验证与边界

本次收尾新鲜检查：两个工作树开始时干净，远端分支已 fetch，两个远端 HEAD 均为当前开发提交的祖先；累计 `git diff --check` 通过。只更新交接文档与状态，不重复构建与媒体测试。

沿用本阶段已执行的验证：5 项最终侧栏测试和生产构建通过；Electron 原生 Gate 已验证浅深色、1980/1440/720 窗口设置底边距 16px、可点击、播放器居中且控件无重叠，并取得完整 `OPEN_LIBRARY_NATIVE_PASS`。截图已目视检查，目录 `/Volumes/LifeWeave/Developer/CommandLine/tmp/musicbridge-sidebar-bottom-ui/`。之前播放器交互的 47 项定向测试及类型检查等证据见分项报告，均不是本次重新执行。

Owner 本次确认覆盖当前 UI 里程碑。隔离合成 Gate 仍不代表真实音频、设备、录音或发布验收；历史远程音频、参考图变体等 carryover 未重新判定。用户实例未重启，数据未迁移，其他工作树未修改。远端同步不等于 CI 通过或软件发布。

## 下一阶段

实物收藏 UI 重制：从此里程碑继续，先独立 HTML 预览并收集具体视觉反馈，再迁入正式应用。本次仅记录方向，未开始下一阶段实现、未创建新任务。既有库存、磁带参考目录、照片、Roon 关联和录音数据继续保留，不默认改业务合同或做数据迁移。
