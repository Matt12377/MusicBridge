# TASK-060：录音参数与持久化执行资产

## 身份与结论

- 基线：`d3c093947a3661da684e7780e49eb2ebd4a5b46a`（TASK-059 最终状态锁定）。
- 分支：`codex/task-060-v3-execution-assets`。
- 实现：`84dd9630b0616573a2e514474db80d3b3a704aa2`。
- 报告提交：包含本文件的独立提交；精确 SHA 在下一状态锁定提交写入 `project/STATUS.json`。
- 结论：本地自动验证通过，完整 V3 未完成。0 子代理；未 push、未集成 main、未连接真实账号/Roon/输出设备，未获得 Owner 验收。

## 交付范围

Profile 保存有序设备链、默认参数、预卷、介质兼容及显式执行格式；修改产生不可变版本。本次参数使用明确版本、修订号和三态覆盖（继承/未设定/另填），支持临时链，旧参数与资产不随模板修改。Profile 和 Session 分别使用事务、CAS 与幂等命令账本；schema 11/12 失败整体回滚。

执行提案重新绑定 Frozen M/L、实际来源、介质预留和本次参数。用户确认前不写文件。Direct 使用 TASK-059 同格式整数 PCM 内核，排他写入逐面 WAV；Prepared 校验并引用已保留的原始 Render，不复制音频、不重复加 Gap；空 B 无占位文件。

执行任务保存意图、逐面进度、清单和发布状态。文件与 Manifest 完整核验后才提交不可变资产。取消、撤权和故障不自动删除；冷启动仅补交完整已发布产物，不重编译。历史发布事实与带时间戳的当前文件验证分别展示。

Main/Preload/utility IPC 与既有录音页面已接通。明确确认格式、目标、帧数和未正式就绪状态；未知回执保留原命令并禁用关闭；读取可独立取消；刷新保留编辑；关闭后恢复入口焦点。

## 最终验证

实测 Node `22.23.2`、pnpm `10.17.1`。35 个代码/测试路径的 SHA-256 在各 Gate 后保持一致。

| Gate | 结果 | 退出码 |
|---|---|---|
| 根 verify | typecheck / test / build 通过 | 0 |
| Contracts | 53/53 | 0 |
| Core | 624/624 | 0 |
| Desktop | 169/169 | 0 |
| Security | 22/22 | 0 |
| Electron 生命周期 | 4/4 | 0 |
| Playwright | 48/48，约 5.4 分钟，无失败重试 | 0 |
| 控制面 / 边界 / 循环 | PASS，140 个源文件 | 0 |
| WAVE-5 身份 / 候选 / 允许路径 | PASS | 0 |
| git diff --check | PASS | 0 |

控制面脚本仍核对旧 WAVE-3，因此另行验证 WAVE-5 的任务、分支、base 和允许路径；不以旧脚本 PASS 替代当前身份。

## 实际文件与桌面证据

最终 Direct 合成 WAV 由 Python 标准库 wave 独立读取：44.1 kHz、双声道、16 bit，396902 帧、1587652 字节。逐段是 44100 帧 Lead-in、44101 帧源、220500 帧数字零 Gap、44101 帧源、44100 帧 Tail。源 PCM 完全一致；2000ms 手动预卷没有再写入音频。

- 音频 SHA-256：`83793ec45f3eae17cd5b29a5de1116fe166c85c8fcc2c61499e2d90f298e338a`。
- PCM SHA-256：`31ffd64eb06e9673645b5ada9b9248d24def52c190fe48eaa2441a92d7887832`。
- 最终 Manifest SHA-256：`f38fb9da438bb852897ff23ad66f121d45d555540c31add0a29b17f649a43bf8`。

真实 Electron IPC 合成流程覆盖参数保存、版本隔离、临时链、读取取消、执行/Session 回执丢失重试、未保存编辑保护、冷启动历史和文件篡改后的拒绝。PREP 双面/空 B 场景核对原件字节及引用帧数；Core 另覆盖 DAT、超时、磁盘满、目录撤权和发布后 DB 故障恢复。

1440×900 与 720×480 面板无横向溢出，axe critical/serious 为零；实际查看最终参数与历史截图。自动视觉检查不代替 Owner 美观/操作体验验收。

## RED 与修复

1. 初始执行面板入口不存在，SQLite 库存事务、合成来源和冻结 M/L 前置成功后按钮查找失败；接入后闭环通过。
2. 错误原因数组被 String 转换接受；严格字符串枚举的反例先失败后通过。
3. 历史读取失败后刷新覆盖未保存电平；改为保留编辑、刷新保存基线并清除确认。
4. 最终截图显示发布后残留旧运行进度；保留旧启动回执稳定复现后，改为“请求已接受”，实时进度只从任务记录读取。

Profile 下拉框的精确标签定位失败属于测试定位问题，单独记录，不冒充产品行为 RED。超时、磁盘满和事务故障由受控注入覆盖，不是硬件实测。

## 审查

按 SPEC 后 QUALITY 自查，0 子代理；不是独立审查。

| Before | After | 原因 |
|---|---|---|
| 核心编译没有正式桌面入口 | 在既有录音上下文提供参数与执行面板 | 不新增设备页或侧栏 |
| 刷新会丢未保存参数 | 保留输入并要求重新确认 | 防止无意覆盖 |
| 旧回执呈现为持续进度 | 回执与实时任务进度分开 | 避免发布后仍显示运行 |
| 历史完成状态缺少当前验证 | 独立重新验证及时间戳 | 不将旧 Hash 等同当前可用 |

证据位于忽略目录 `reports/runtime/task-060-_7ztk4bn`。最终候选为 `final-code-candidate.json`，Gate 结果为 `final-gate-exits.json`，最终截图和合成音频在 `test-results-final/`，独立复核为 `independent-pcm-final.json`。前轮完整通过、单项 RED/GREEN 与最终候选分开保存。

## 后续与保留边界

转换器/SRC/独立 Derivative、正式 Profile Snapshot/RecordingPlan、输出后端与 Gate B、录音三层完成确认、归档/双库/Replica/J-Card/备份、参考目录/Excel/Want List 及全链路 Owner 验收继续保留。

F-01 未获决定，不自动清理执行音频，不承诺 Direct 自包含永久档案。通用跨 Renderer/应用重启 outbox 仍未完成，会话内重试不能替代。真实 Source Roots、设备、接线、停止延迟、Provider/Roon、完整 A～E、Owner、main 集成及签名安装/发布均保持独立门槛。

下一阶段从本任务最终状态锁定 HEAD 建立独立分支继续，不从未提交 WIP 或实现提交跳接。TASK-047 真实验收和旧视觉 carryover 不自动关闭。
