# ADR-031：明确录音工作上下文与局部关系选曲

状态：TASK071本地自动验证通过，Owner未验收。基线72db8616ddbb461b93e9ffa960576af052c2bdf6。

Source Picker局部关系入口读取已登记矩阵和数字详情，只使用当前运行引用；Exact/Probable/Related、已确认缺少和未核实不合并。关系不是音频文件证据，不产生Source Lock。冷启、离线、运行引用变化保留本地关系，已选运行引用必须重新核对，不自动重定位或播放；只有用户明确确认才复用原append/outbox写草稿。

下一步是Renderer只读引导，不是正式RecordingPlan状态机。六类事实统一由带代际标记的控制器读取；源标签只在同一ready事实中显示资格，旧请求不能覆盖另一草稿。读取失败保留历史语义但不继续宣称当前就绪。草稿pending/dirty优先，必填校验仍有效。

历史集合不代表用户本次意图。媒体规划、冻结布局、处理路径、Logic工作区及PREP必须显式选择；布局决定所关联母版，源hash/帧数/规格/预留与谱系均需一致。改变上游选择清除下游，刷新只保留仍一致的选择，重开/切库/草稿revision变更清空临时上下文。下一步按钮把已选plan/layout/path/PREP传入既有工具，失效初始编号留空并明确报错，不回退第一条历史；手动工具入口不复用上次CTA参数。事实刷新发布新状态引用，确保子组件同步显示选择项。五个工具记录实际触发控件；关闭刷新后只在同一草稿代际且用户尚未键盘/指针交互、焦点仍在原控件或body时恢复，卸载清理监听。按钮只打开既有工具，不自动预检、预留、导出、编译或开始录音；正式Preflight受TASK072/F01约束。

照片img的lazy属性不能推迟获取data URL，因此在接近可视区域时才发既有API读取。id/loader/重试/卸载均有代际隔离。卡片中的照片保持非交互，独立大图及没有外层按钮的媒体候选照片提供单图重试，避免嵌套按钮；原比例、来源说明、照片字节和持久模型不变。

不增加公开合同、schema、永久侧栏或照片墙设计，不扩真实资料和硬件权限。测试合成Roon及图片只证明软件行为，不代替Owner验收。
